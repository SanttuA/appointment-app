# Tasks

- [x] Create pnpm monorepo and shared tooling.
- [x] Add Fastify API with Prisma schema, auth, RBAC, scheduling, and seed data.
- [x] Add Next.js localized UI with English and Finnish routes.
- [x] Add Vitest and Playwright coverage for core behavior.
- [x] Add Docker Compose production setup.
- [x] Add GitHub Actions CI for pull requests to `master`.

Future hardening:

- Add email invitations and password reset.
- Add notification delivery.
- Run the same integration suite against PostgreSQL before database migration.
- Add production TLS proxy when a deployment hostname exists.
